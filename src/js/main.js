// funções ou classes que não manipulam o DOM diretamente (ex.: formatadores de dada, funções de validação simples).
// Funções responsáveis por fazer chamadas http (ex.: fetch, axios) para o backend.
